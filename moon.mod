name = "dowdiness/rle"

version = "0.2.3"

import {
  "moonbitlang/quickcheck@0.14.0",
}

readme = "README.md"

repository = "https://github.com/dowdiness/rle"

license = "Apache-2.0"

keywords = [ "rle", "run-length-encoding", "data-structure" ]

description = "Generic run-length encoded sequence with O(log n) position lookup"
